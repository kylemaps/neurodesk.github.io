{{ $data := dict }}
{{ $url := "js/applist.json" }}
{{ $resource := resources.Get $url }}
{{ if $resource }}
  {{ $data = $resource | transform.Unmarshal }}
{{ else }}
  {{ errorf "Unable to get resource %q" $url }}
{{ end }}
{{ $data | jsonify }}